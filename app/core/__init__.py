"""Cross-cutting infrastructure shared across PulseForge.

Owns logging today, and later errors, correlation IDs, config, and hashing
(ARCHITECTURE.md). This package imports nothing from the rest of ``app/``.
"""
